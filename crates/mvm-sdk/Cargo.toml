[package]
name = "mvm-sdk"
description = "Build-time Rust SDK for mvm — declare workloads, emit canonical IR. Ported from mvmforge-sdk per plan 60 Phase 5."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
mvm-ir = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = "1"

[lints]
workspace = true
