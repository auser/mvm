.build/
.swiftpm/
*.xcodeproj

# Intentionally NOT ignored: Package.resolved. Plan 97 Phase B says
# Swift PM's lockfile-equivalent must be checked in (the Swift parallel
# to Cargo.lock — ADR-002 claim 7 / reproducibility). The package has
# no remote dependencies today so the file doesn't exist; uncomment-
# free-ness here means it'll be committed automatically if a dep is
# added.
