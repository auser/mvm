[package]
name = "mvm-runner"
description = "Hardened in-guest entrypoint runtime for mvm function-call workloads. Ported from mvmforge-runtime per plan 60 Phase 5 (Slice C)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "mvm-runner"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
libc = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
