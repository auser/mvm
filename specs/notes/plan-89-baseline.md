# Plan 89 W1 — cold-boot telemetry baseline

Generated by `scripts/plan-89-baseline.sh`. Run on each target
platform (macOS Apple Silicon and Linux KVM) and append the section
to this file. The headline number is the median `job_start_ms` —
the wall-clock from init anchor to the user's build script's first
instruction. Plan 89 §Order of operations gates the rest of the
plan on this number exceeding ~500 ms on both platforms.

<!--
  Sections below get appended by the harness. Keep this header in
  place — the harness only appends; it doesn't rewrite the doc.

  To run:
    ./scripts/plan-89-baseline.sh                 # default: 5 runs against tests/fixtures/plan-89-baseline
    ./scripts/plan-89-baseline.sh --runs 10
    ./scripts/plan-89-baseline.sh --flake ./my-flake

  The default flake at tests/fixtures/plan-89-baseline is
  intentionally a throw-on-eval flake — see its description for
  rationale. The harness tolerates the inner `nix build` failing
  because boot-timings.json is written BEFORE cmd.sh runs, so all
  the boot fan-out phases the plan cares about
  (init_start_ms → job_start_ms) are captured regardless.

  After both macOS and Linux runs are in, fill out §Decision below.
-->

## Decision

_Waiting for measurement runs on both platforms._

- macOS Apple Silicon: _to be filled in_
- Linux KVM: _to be filled in_

Plan 89's §Order of operations rule: if **both** medians fall under
500 ms, the rest of the plan is not worth shipping. If either is
over, W2 (vsock dispatch wire) becomes the next PR.

---
